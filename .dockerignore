# Git
.git
.gitignore

# Persistence data (container starts fresh or uses volumes)
persistence/

# Julia manifest (regenerated inside container for correct Julia version)
Manifest.toml

# Docker files (not needed inside the image)
Dockerfile
docker-compose.yml
.dockerignore

# Documentation site build artifacts
docs/_site/

# Benchmark results (generated, not needed in image)
benchmarks/results/

# IDE / OS artifacts
.DS_Store
.kiro/
